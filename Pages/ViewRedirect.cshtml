@page
@model Beryl.Pages.ViewRedirectModel

@{
    ViewData["Title"] = "Link";
}

@section Scripts {
    @if (Model.RedirectTimeSeconds != null)
    {
        var c = $"{Model.RedirectTimeSeconds};URL='{Model.RedirectLink.Url}'";
        <meta http-equiv="refresh" content="@c" />
    }
}

<pre><code>
@Model.RedirectLink.Description
</code></pre>
